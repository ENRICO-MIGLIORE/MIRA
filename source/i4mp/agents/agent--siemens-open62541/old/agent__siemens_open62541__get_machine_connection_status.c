#include "agent__siemens_open62541__private.h"


int agent__siemens_open62541__get_machine_connection_status (int md, char* buffer, size_t buffer_size, size_t* buffer_len)
{

	//int rc;
	//
	//rc = subagent__get_machine_connection_status(&agent_so->subagent.machine[md], buffer, buffer_size, buffer_len);

	//return rc;

	return 0;
}

