#include "machine.h"


int delegate__private__read_part_program_name__simulated (subagent__machine_t* machine, int md)
{

	//if ((md < 0) || (md >= machine->machine_count))
	//{
	//	return -1000;
	//}


	//machine->machine_local[md].part_program_name_valid = 0;


	//if (machine->machine_local[md].part_program_name_simulated_valid != 1)
	//{
	//	return -100;
	//}

	//strcpy(machine->machine_local[md].part_program_name, machine->machine_local[md].part_program_name_simulated);
	//strcpy(machine->machine_local[md].part_program_name_utf_8, machine->machine_local[md].part_program_name);
	//machine->machine_local[md].part_program_name_utf_8_len = strlen(machine->machine_local[md].part_program_name_utf_8);

	//machine->machine_local[md].part_program_name_valid = 1;

	return 0;

}

