#include "siemens_open62541__private.h"


int agent__siemens_open62541__private__read_part_program_name__simulated (int md)
{


	//if ((md < 0) || (md >= so->machine_count))
	//{
	//	return -1000;
	//}


	//so->machine[md].part_program_name_valid = 0;


	//if (so->machine[md].part_program_name_simulated_valid != 1)
	//{
	//	return -100;
	//}

	//strcpy(so->machine[md].part_program_name, so->machine[md].part_program_name_simulated);
	//strcpy(so->machine[md].part_program_name_utf_8, so->machine[md].part_program_name);
	//so->machine[md].part_program_name_utf_8_len = strlen(so->machine[md].part_program_name_utf_8);

	//so->machine[md].part_program_name_valid = 1;

	return 0;


}

