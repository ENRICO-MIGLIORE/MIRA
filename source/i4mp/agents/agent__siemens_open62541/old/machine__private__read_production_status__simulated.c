#include "machine.h"


int delegate__private__read_production_status__simulated (subagent__machine_t* machine, int md)
{

	//if ((md < 0) || (md >= machine->machine_count))
	//{
	//	return -1000;
	//}

	//machine->machine_local[md].production_status_valid = machine->machine_local[md].production_status_valid_simulated;
	//machine->machine_local[md].production_status = machine->machine_local[md].production_status_simulated;

	return 0;
}

