#include "siemens_open62541__private.h"


int agent__siemens_open62541__private__read_connection_status__simulated(int md)
{

	//if ((md < 0) || (md >= so->machine_count))
	//{
	//	return -1000;
	//}

	//so->machine[md].connection_status_valid = so->machine[md].connection_status_valid_simulated;
	//so->machine[md].connection_status = so->machine[md].connection_status_simulated;

	return 0;

}

