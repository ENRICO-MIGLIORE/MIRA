#include "agent__siemens_open62541__private.h"


//extern const char* agent_so__agent_name;


int agent__siemens_open62541__get_agent_name (char* buffer, size_t buffer_size, size_t* buffer_len)
{
	//size_t len;


	//if (buffer == NULL)
	//{
	//	return -100;
	//}

	//if (buffer_len == NULL)
	//{
	//	return -100;
	//}

	//len = strlen(agent_so__agent_name);
	//if (len >= buffer_size)
	//{
	//	return -100;
	//}

	//strcpy(buffer, agent_so__agent_name);

	//*buffer_len = len;

	return 0;
}