#include "machine.h"


void delegate__private__task__read_machines_data (subagent__machine_t* machine)
{

	int i;

	//for (i = 0; i < machine->machine_count; i++)
	//{

	//	/*
	//	 * production status
	//	 */
	//	if (machine->machine_local[i].config_2.simulated == 1)
	//	{
	//		//(void) delegate__private__read_connection_status__simulated(delegate, i);

	//		//(void) delegate__private__read_production_status__simulated(delegate, i);

	//		//(void) delegate__private__read_alarm_status__simulated(delegate, i);

	//		//(void) delegate__private__read_part_counter__simulated(delegate, i);

	//		//(void) delegate__private__read_cycle_time__simulated(delegate, i);

	//		//(void) delegate__private__read_part_program_name__simulated(delegate, i);
	//	}
	//	else
	//	{
	//		//(void) delegate->config.read_connection_status(i);

	//		//(void) delegate->config.read_production_status(i);
	//	
	//		//(void) delegate->config.read_alarm_status(i);

	//		//(void) delegate->config.read_part_counter(i);

	//		//(void) delegate->config.read_cycle_time(i);

	//		//(void) delegate->config.read_part_program_name(i);

	//	}
	//}

}


